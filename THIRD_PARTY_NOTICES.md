# 第三方材料与许可边界

本文件是第三方来源清单，不构成法律意见，也不会自动授予任何数据、照片、模型权重或依赖的使用许可。当前仓库尚未附加根开源许可证；按照默认版权规则，公开可见不等于授予复制、修改、再分发或商业使用许可。如需开源，应由仓库所有者在确认第三方边界后另行选择许可证。第三方材料始终以各自上游条款为准。

## 1. JavaScript 依赖

根据本仓库当前安装包的 package.json：

| 包 | 版本 | 上游声明许可 |
| --- | ---: | --- |
| onnxruntime-web | 1.27.0 | MIT |
| React | 19.2.6 | MIT |
| React DOM | 19.2.6 | MIT |
| Next.js | 16.2.6 | MIT |
| Vite | 8.0.13 | MIT |
| vinext | 0.0.50 | MIT |
| Wrangler | 4.92.0 | MIT OR Apache-2.0 |

完整直接与传递依赖及精确版本见 package-lock.json。再分发构建产物时，应保留各包要求的版权与许可文本；本表不能替代 package-lock 和上游许可证。

浏览器运行时从固定版本 jsDelivr 地址按需加载 onnxruntime-web 1.27.0 的 mjs/wasm 文件。CDN 只接收公开运行库资源请求；应用代码不会把题图像素发送到该 CDN。

## 2. Python 依赖

ml/requirements.txt 声明 PyTorch、torchvision、Pillow 和 NumPy。ONNX 导出脚本还导入 onnx。实际安装版本、CUDA wheel 和许可证应以部署环境的 lock/metadata 及上游条款为准；当前 requirements 文件使用最低版本范围，不能作为精确第三方清单。

## 3. PGDP5K

用途：主要真实几何图数据、训练/验证/测试掩码转换来源。

仓库记录的来源：

- 镜像：https://huggingface.co/datasets/PeijieWang/PGDP5K
- 原始页面：https://nlpr.ia.ac.cn/databases/CASIA-PGDP5K/

当前存在需要保守处理的条款差异：

- Hugging Face 镜像标注 MIT；
- 原始 CASIA 页面说明学术研究在协议下免费，商业使用需要联系。

因此：

- 原始 PGDP5K 数据不应随本仓库提交；
- 商业使用和公开再分发前必须向原始权利方核实；
- 不能只根据镜像标签假设所有衍生物都可商业使用；
- 由 PGDP5K 训练得到的 checkpoint/ONNX 权重也需要单独审查再分发边界。

本仓库只提交转换代码和评估报告，不提交 PGDP5K 原始/转换数据，也不公开提交由 PGDP5K 训练得到的 checkpoint/ONNX 权重。本地工件被 `.gitignore` 排除；这不代表其商业使用许可已经得到确认。

## 4. Geometry3K

用途：结构化点、线、圆注释的转换与验证探索。

来源：https://huggingface.co/datasets/LibraTree/geometry3k

本地记录显示数据卡许可为 other。原始数据或转换副本在条款确认前不应公开再分发。本仓库不提交该数据集。

## 5. Geo170K

用途：作为较大的图像/文本预训练与描述语料候选；不是当前六分类像素掩码的直接标注集。

来源：https://huggingface.co/datasets/Luckyjhg/Geo170K

本地记录显示其数据卡声明 MIT。使用或再分发时仍应重新核对上游最新条款和数据中可能包含的第三方内容。本仓库不提交该数据集。

## 6. 示例照片与回归夹具

原始回归照片来自用户提供的教材页面，用于复现手写笔迹、角标、旋转、漏线和题干约束问题。当前资料没有证明教材页面内容、拍摄内容或照片具备公开再分发许可，因此公开分支已移除 `public/figure2-photo.jpg` 与 `tests/fixtures/figure2_photo.jpg`，并通过 `.gitignore` 防止本地副本被误提交。

网页中的图2按钮改为根据已核对点线拓扑在浏览器内重新绘制的原创示例。GPU 照片回归测试要求通过 `--image` 显式传入使用者有权使用的本地图片。

## 7. 模型权重

本地已验证、但不随公开分支分发的浏览器权重：

- public/models/geometry_unet_pgdp5k_epoch5_320.onnx（被 `.gitignore` 排除）
- SHA-256：2548477e22be4965d38cb7d963f0504e05143616eef866746fa776219adb865a

该模型由项目代码训练，但训练数据包含第三方来源。代码许可不会自动覆盖模型权重，数据许可也不一定自动覆盖衍生权重。公开发布、应用商店分发或商业服务前，应分别确认：

- 训练数据协议是否允许权重再分发；
- 是否要求署名、研究用途限制或商业授权；
- 示例图片是否随模型/应用一起分发；
- CDN 与运行库许可是否得到保留。

## 8. 发布前检查清单

- [ ] 根许可证已明确覆盖哪些自有代码；
- [ ] PGDP5K 原始权利方条款已核实；
- [ ] 模型权重是否可以公开和商业再分发已核实；在此之前保持本地排除；
- [x] 教材照片已从公开分支移除，并替换为浏览器内自绘示例；
- [ ] 没有提交原始/转换数据、缓存或本地绝对路径；
- [ ] 第三方依赖许可文本按要求随构建产物保留；
- [ ] 隐私与手写内容已审查；
- [ ] README 与应用页面没有夸大 OCR、通用准确率或 Apple Neural Engine 能力。
